# Sky Flight Test 🚀

An endless runner flight action game written in Python 3 using Pygame.

## Prerequisites
- Python 3.8+
- Pygame (`pip install pygame`)

## Setup & Run
1. Install dependencies:
   ```bash
   pip install pygame